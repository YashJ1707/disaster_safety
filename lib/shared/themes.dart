import 'package:flutter/material.dart';

class Consts {
  static const Color kprimary = Color(0xff00000);
  static const Color kwhite = Color(0xfffffff);

  Widget h1(String label) {
    return Text(label);
  }
}
