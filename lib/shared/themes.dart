import 'package:flutter/material.dart';

class Consts {
  static const Color kprimary = Color.fromARGB(255, 107, 0, 116);
  static const Color klight = Color.fromARGB(255, 249, 181, 255);
  static const Color kblack = Color(0xff212121);
  // static const Color kwhite = Color.fromARGB(15, 255, 248, 248);
  static const Color kwhite = Colors.white;
  static const Color kdark = Color(0xff212121);

  Widget h1(String label) {
    return Text(label);
  }
}
