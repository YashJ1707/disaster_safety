import 'package:flutter/material.dart';

class RaiseIncidentPage extends StatelessWidget {
  const RaiseIncidentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
